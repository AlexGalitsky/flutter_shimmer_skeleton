<div align="center">
  <img src="images/logo.png" alt="flutter_shimmer_skeleton" width="200"/>
</div>

# flutter_shimmer_skeleton

<!-- TEST_BADGE_START -->
![Tests](https://img.shields.io/badge/tests-passing-success?style=flat-square)
<!-- TEST_BADGE_END -->

Flutter пакет, предоставляющий виджеты скелетонов с эффектом шиммера на основе [официальной документации Flutter](https://docs.flutter.dev/cookbook/effects/shimmer-loading).

## Возможности

- ✨ Анимация Shimmer эффекта
- 🎨 Настраиваемые цвета градиента
- 📦 Готовые к использованию виджеты скелетонов:
  - `SkeletonBox` - Прямоугольные плейсхолдеры
  - `SkeletonCircle` - Круглые плейсхолдеры (для аватаров)
  - `SkeletonText` - Текстовые плейсхолдеры с несколькими строками
  - `SkeletonListItem` - Элемент списка с аватаром и текстом
  - `SkeletonCard` - Карточка с изображением и текстом
- 🎯 Простота использования и интеграции
- 📱 Работает на всех платформах Flutter

## Установка

Добавьте это в файл `pubspec.yaml` вашего пакета:

```yaml
dependencies:
  flutter_shimmer_skeleton: ^0.0.1
```

Затем выполните:

```bash
flutter pub get
```

## Использование

### Базовый пример

Оберните ваши виджеты виджетом `Shimmer` и используйте `ShimmerLoading` для условного применения эффекта шиммера:

```dart
import 'package:flutter_shimmer_skeleton/flutter_shimmer_skeleton.dart';

Shimmer(
  linearGradient: LinearGradient(
    colors: [
      Colors.grey[300]!,
      Colors.grey[100]!,
      Colors.grey[300]!,
    ],
    stops: [0.1, 0.3, 0.4],
    begin: Alignment(-1.0, -0.3),
    end: Alignment(1.0, 0.3),
    tileMode: TileMode.clamp,
  ),
  child: YourWidget(),
)
```

### Использование ShimmerLoading

```dart
ShimmerLoading(
  isLoading: true,
  child: Container(
    width: 200,
    height: 100,
    color: Colors.grey,
  ),
)
```

### Использование виджетов скелетонов

#### SkeletonBox

```dart
SkeletonBox(
  width: 200,
  height: 100,
  borderRadius: BorderRadius.circular(8),
)
```

#### SkeletonCircle

```dart
SkeletonCircle(
  size: 50,
)
```

#### SkeletonText

```dart
SkeletonText(
  lines: 3,
  lineHeight: 16,
  spacing: 8,
  lastLineWidth: 250, // Последняя строка может быть короче
)
```

#### SkeletonListItem

```dart
SkeletonListItem(
  hasAvatar: true,
  hasSubtitle: true,
  avatarSize: 48,
  titleWidth: 200,
  subtitleWidth: 150,
)
```

#### SkeletonCard

```dart
SkeletonCard(
  hasImage: true,
  imageHeight: 200,
  hasText: true,
  textLines: 2,
)
```

### Полный пример

```dart
import 'package:flutter/material.dart';
import 'package:flutter_shimmer_skeleton/flutter_shimmer_skeleton.dart';

class MyWidget extends StatefulWidget {
  @override
  _MyWidgetState createState() => _MyWidgetState();
}

class _MyWidgetState extends State<MyWidget> {
  bool isLoading = true;

  static const _shimmerGradient = LinearGradient(
    colors: [
      Color(0xFFEBEBF4),
      Color(0xFFF4F4F4),
      Color(0xFFEBEBF4),
    ],
    stops: [0.1, 0.3, 0.4],
    begin: Alignment(-1.0, -0.3),
    end: Alignment(1.0, 0.3),
    tileMode: TileMode.clamp,
  );

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Shimmer(
        linearGradient: _shimmerGradient,
        child: ListView(
          children: [
            SkeletonListItem(
              hasAvatar: true,
              hasSubtitle: true,
            ),
            SkeletonCard(
              hasImage: true,
              hasText: true,
            ),
            SkeletonText(
              lines: 3,
            ),
          ],
        ),
      ),
    );
  }
}
```

## Справочник API

### Shimmer

Основной виджет, предоставляющий эффект шиммера для своих дочерних виджетов.

**Свойства:**
- `linearGradient` (обязательный) - Градиент, используемый для эффекта шиммера
- `child` - Дочерний виджет, к которому применяется эффект шиммера
- `period` - Длительность одного цикла анимации шиммера (по умолчанию: 1000ms)

### ShimmerLoading

Виджет, который условно применяет эффект шиммера к своему дочернему виджету.

**Свойства:**
- `isLoading` (обязательный) - Должен ли быть активен эффект шиммера
- `child` (обязательный) - Дочерний виджет, к которому применяется эффект шиммера

### SkeletonBox

Прямоугольный плейсхолдер скелетона.

**Свойства:**
- `width` - Ширина блока
- `height` - Высота блока
- `borderRadius` - Радиус скругления углов блока
- `color` - Базовый цвет (по умолчанию: Colors.black)
- `isLoading` - Активен ли шиммер (по умолчанию: true)

### SkeletonCircle

Круглый плейсхолдер скелетона.

**Свойства:**
- `size` (обязательный) - Диаметр круга
- `color` - Базовый цвет (по умолчанию: Colors.black)
- `isLoading` - Активен ли шиммер (по умолчанию: true)

### SkeletonText

Текстовый плейсхолдер скелетона с несколькими строками.

**Свойства:**
- `lines` - Количество строк (по умолчанию: 1)
- `lineHeight` - Высота каждой строки (по умолчанию: 16)
- `spacing` - Расстояние между строками (по умолчанию: 8)
- `width` - Ширина строк
- `lastLineWidth` - Ширина последней строки
- `borderRadius` - Радиус скругления углов каждой строки
- `color` - Базовый цвет (по умолчанию: Colors.black)
- `isLoading` - Активен ли шиммер (по умолчанию: true)

### SkeletonListItem

Скелетон элемента списка с аватаром и текстом.

**Свойства:**
- `hasAvatar` - Показывать ли аватар (по умолчанию: true)
- `hasSubtitle` - Показывать ли подзаголовок (по умолчанию: true)
- `avatarSize` - Размер аватара (по умолчанию: 48)
- `titleWidth` - Ширина строки заголовка
- `subtitleWidth` - Ширина строки подзаголовка
- `spacing` - Расстояние между аватаром и текстом (по умолчанию: 12)
- `color` - Базовый цвет (по умолчанию: Colors.black)
- `isLoading` - Активен ли шиммер (по умолчанию: true)

### SkeletonCard

Скелетон карточки с изображением и текстом.

**Свойства:**
- `hasImage` - Показывать ли изображение (по умолчанию: true)
- `imageHeight` - Высота изображения (по умолчанию: 200)
- `imageAspectRatio` - Соотношение сторон изображения
- `hasText` - Показывать ли текст (по умолчанию: true)
- `textLines` - Количество текстовых строк (по умолчанию: 2)
- `padding` - Отступы вокруг содержимого карточки (по умолчанию: EdgeInsets.all(16))
- `borderRadius` - Радиус скругления углов карточки (по умолчанию: BorderRadius.all(Radius.circular(12)))
- `color` - Базовый цвет (по умолчанию: Colors.black)
- `isLoading` - Активен ли шиммер (по умолчанию: true)

## Настройка

Вы можете настроить эффект шиммера, предоставив свой собственный градиент:

```dart
Shimmer(
  linearGradient: LinearGradient(
    colors: [
      Colors.blue[300]!,
      Colors.blue[100]!,
      Colors.blue[300]!,
    ],
    // ... другие свойства градиента
  ),
  period: Duration(milliseconds: 1500), // Пользовательская скорость анимации
  child: YourWidget(),
)
```

## Вклад в проект

Вклад в проект приветствуется! Пожалуйста, не стесняйтесь отправлять Pull Request.

## Лицензия

Этот проект лицензирован под MIT License - см. файл LICENSE для подробностей.

## Благодарности

Этот пакет основан на примере из официальной документации Flutter:
https://docs.flutter.dev/cookbook/effects/shimmer-loading
